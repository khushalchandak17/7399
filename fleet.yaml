# Default: default
defaultNamespace: default

helm:
  releaseName: web



- name: prod
  helm:
    chart: https://charts.bitnami.com/bitnami/nginx-15.4.4.tgz
  clusterSelector:
    matchLabels:
      example.com: prod

